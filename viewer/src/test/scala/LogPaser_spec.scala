import org.btproject.test.SpecHelper
import org.btproject.model._

class LogFilePaserSpec extends SpecHelper {
  describe("Paser"){
    it("should search logedby")(pending)
    it("should return unknown BDA when notfound logedby ")(pending)
  }
}
