ホームフォルダに解答
unzip logger.zip

GUI - 端末内で実行する
プロキシの設定がされていない場合は接続できません

ターミナルから
export http_proxy=http://proxy.ksc.kwansei.ac.jp:8080/
画面の指示に従ってYとかおす

内部の処理で管理者権限が必要なところではパスワードを要求される


画面の明るさは設定中のためそのままです
使用時は上部メニューバーの太陽のアイコンをタッチし
-側へドラッグするか-を何度か押してください

Bluetoothの名前の設定
ユーザ名・他人のログに出てくる名前を設定します
$ sudo hciconfig hci0 name <device name>


ロガーの使い方


送信ツールの使い方
ruby send_log.rb
同フォルダのkeyファイルを参照しています。
